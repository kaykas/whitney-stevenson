# ERRORS

Failures captured as rules. Read the last 5 entries before touching the site.

Format: `[YYYY-MM-DD] | [what broke] | [root cause] | [rule]`

Append to the bottom on every new failure. Don't edit history — rules accumulate.

---

[2026-05-02] | (seeded — no incidents recorded yet) | n/a | Append below as failures occur. Per Jascha's collaboration playbook: every mistake becomes a rule before moving on. Two prior pieces of guidance baked into CLAUDE.md (not failures, but worth re-stating): (1) deploy to Vercel prod after every iteration so Whitney can verify; (2) don't ask Whitney/Jascha for assets they aren't producing — execute on shell + assets I have.

[2026-09-06] | Ahrefs/GSC flagged the homepage (`/`) with "Page has no outgoing links" (Error severity) | `src/app/page.tsx` shipped with only two anchors: a same-page fragment (`#chapters`) and a `mailto:`. Crawlers count neither as an outgoing link, so the homepage was a dead end — no crawl path from `/` to `/blog` or any post, and no external corroborating link. The blog index and post templates already linked correctly; only the homepage was orphaned. | RULE: every route must ship at least one crawlable `<a href>`/`<Link>` to another URL — fragment-only anchors and `mailto:` don't count. When adding a route or a content section, wire it into the internal link graph in both directions (home → blog → post → home) and give at least one external `rel="me"` link to a verified profile. Never invent a social URL to satisfy this; link only profiles that exist (the placeholder "TW" span was replaced with a real `/blog` link rather than a fabricated Twitter URL). Verify after build with: `grep -o 'href="[^"]*"' .next/server/app/index.html`.
[2026-09-07] | Ahrefs/GSC flagged `/` with "Page has no outgoing links" (Error severity) | The homepage's only anchors were a same-page fragment (`#chapters`) and a `mailto:` — neither counts as an outgoing link to a crawler. `/blog` and all five posts were reachable only via sitemap.xml, never via an on-page link, so the whole Field Notes section was effectively orphaned. | Every page must carry at least one real crawlable `href` to another URL. Fragment-only and `mailto:` links do not count. Sitemap inclusion is not internal linking — if a route is in `sitemap.ts`, some rendered page must also link to it. A sitewide footer (`src/components/SiteFooter.tsx`, mounted in `layout.tsx`) is the guardrail; don't remove it. Verify after any nav/layout change by grepping hrefs out of the prerendered HTML: `grep -o 'href="[^"]*"' .next/server/app/index.html`.

[2026-09-09] | Ahrefs/GSC flagged "Page has no outgoing links" (Error severity, 1 URL) for the third time in four days, after the 09-06 and 09-07 code fixes had already landed and deployed | Not a new code defect. Verified against live production (`curl -A AhrefsBot https://www.whitneystevenson.com/`) and against the prerendered build: `/` ships 12 anchors — 6 crawlable internal routes plus an external `rel="me"` LinkedIn link — and all 8 crawlable routes carry the sitewide footer. The real defect was **process**: the 09-07 rule prescribed a *manual* `grep -o 'href="[^"]*"' .next/server/app/index.html`, which nobody runs on a routine edit, so the site had no way to fail fast and the same Ahrefs finding kept re-firing off crawl data collected before the fix shipped. | RULE: a rule that depends on a human remembering to grep is not a guardrail. The check is now automated — `scripts/check-outgoing-links.mjs`, wired as `postbuild` so it runs inside `npm run build` (locally and on Vercel) and fails the build if any crawlable route has zero internal links to a *different* URL or zero external links. Fragments, self-links and `mailto:` are explicitly discounted, matching how crawlers count. React error boundaries (`_global-error.html`) are excluded — never crawled. Run standalone with `npm run check:links`. Before declaring an SEO error "still broken", check the live HTML first: a recurring report with the fix already deployed is stale crawl data, and re-patching working code is how you break it. ADJACENT FINDING (not fixed here, infra-level): `https://whitneystevenson.com/` → `https://www.whitneystevenson.com/` currently answers **307 temporary**, not the 301 that `vercel.json` requests — the redirect is being served by a Vercel dashboard domain setting that shadows `vercel.json`. A temporary redirect passes no link equity and splits the apex/www signal. Needs fixing in Vercel project → Domains, not in this repo.

[2026-09-09] | Ahrefs/GSC flagged "Meta description too long" (Warning severity, 6 URLs) — `/` plus all five `/blog/*` posts | The `<meta name="description">` for every affected route was 200–308 characters against a ~160-character SERP budget. Root cause was a single overloaded field: `Post.description` in `src/lib/posts.ts` was doing double duty as both the **visible lede** rendered on the post page (`post-lede`) and the blog index, *and* as the meta/OG/Twitter/JSON-LD description. Long dek copy is a legitimate design choice; long meta copy is an SEO defect. Because one field served both, there was no way to fix the meta tag without shortening visible copy, so it was left long. `/` had the same shape — one `DESCRIPTION` const in `layout.tsx` at 218 chars. `/blog` (139) was already fine, which is exactly why the report said 6 URLs and not 7. | RULE: never let one string serve both visible copy and a meta tag — they have different length budgets and different audiences. `Post` now carries a separate required `metaDescription` (≤155 chars) used for `metadata.description`, OpenGraph, Twitter and BlogPosting JSON-LD; `description` stays the visible lede and its length is a design call. Enforced automatically, per the 2026-09-09 outgoing-links precedent that a rule needing a human to remember it is not a guardrail: `scripts/check-meta-descriptions.mjs` reads the **prerendered HTML** (not the source modules, so it catches metadata composed anywhere — layout defaults, route `metadata`, `generateMetadata`) and fails the build if any crawlable route has zero or multiple description tags, or one outside 70–160 chars. HTML entities are decoded before measuring — `&#x27;` is one character to a crawler, not six. Wired as `postbuild` alongside the link check so both run inside `npm run build` locally and on Vercel; standalone via `npm run check:meta`. Negative-tested: restoring a 358-char description fails the build with a non-zero exit.

[2026-09-09] | Ahrefs/GSC flagged 4 URLs with "3XX redirect" (Warning severity) | Two distinct redirect sources, neither visible from the rendered pages. (1) `public/llms-full.txt` published seven **apex** links (`https://whitneystevenson.com/...`) while every other surface — `sitemap.ts`, `robots.ts`, `layout.tsx` metadataBase, every `alternates.canonical`, `JsonLd.tsx`, `llms.txt` — is canonicalised to `https://www.whitneystevenson.com`. Apex answers 307 to www, so each of those published links was a link into a redirect. (2) The apex→www hop is served as a **307 Temporary** by Vercel's domain-level redirect, not by `vercel.json`: main shipped `"permanent": true` (which Vercel emits as 308), yet live apex returns 307 — proof the deployment's route config never sees apex traffic, the project-domain redirect short-circuits it. A temporary redirect tells crawlers *not* to consolidate signal onto www, which is the opposite of the intent. | RULE: `https://www.whitneystevenson.com` is the one canonical origin — every published URL, including the ones in `public/llms.txt` and `public/llms-full.txt`, must be www and must have no trailing slash (`/blog`, never `/blog/`; Next 308s the slashed form). Static text assets under `public/` are part of the link graph and must be audited alongside `.tsx` — `grep -rn 'https://whitneystevenson\.com' src public` must return nothing. `trailingSlash: false` is now pinned in `next.config.ts`; don't flip it. NOTE — apex→www cannot be made permanent from this repo: the redirect lives in the Vercel dashboard (Project → Settings → Domains → whitneystevenson.com), whose status code must be changed from 307 to 301 by hand. `vercel.json` now declares `statusCode: 301` so the repo is correct if apex traffic ever reaches the deployment, but that change alone does not move the live status code. Verify with: `curl -sI https://whitneystevenson.com/ | head -1`.

[2026-09-10] | AEO audit flagged "Inconsistent AI training bot policy" on whitneystevenson.com | `src/app/robots.ts` hand-wrote 14 user-agent groups under two comments, `// AI Trainers — block` and `// AI Citers — allow`, and the comments had drifted from the entries beneath them. Three separate contradictions shipped: (1) `ClaudeBot` was `Disallow` while `anthropic-ai` and `Claude-Web` — the same vendor's retired tokens for the same job — were `Allow`, so an Anthropic crawler got the entire site or none of it depending purely on which UA string it happened to send; (2) `cohere-ai`, a training crawler, sat under the "AI Citers — allow" comment, and `Amazonbot` (Alexa/search, not a trainer) sat under "AI Trainers — block", so both were filed against the wrong stated policy; (3) worst, the policy contradicted the site's own content — `public/llms-full.txt` opens "intended for AI assistants (ChatGPT, Claude, Perplexity, Gemini, etc.) to ingest in full" and ships a "Citation Guidance for AI Assistants" section, while robots.txt blocked `GPTBot`, `ClaudeBot`, `CCBot` and `Google-Extended`. The site was soliciting ingestion in one file and refusing it in another. Separately, `llms-full.txt` wrote all 14 of its self-references against the bare apex host, which only answers a redirect. | RULE: intent that lives in a comment is not enforceable and will drift from the config beneath it — that drift *was* this defect. Policy now lives in `src/lib/bot-policy.ts` as typed data (`token`, `vendor`, `purpose`, `allow`) with the rationale in the module docstring; `robots.ts` only renders it. Resolved in the *allow* direction, because this is a promotional site whose whole AEO strategy is to be ingested and cited — there is no rights-protection motive anywhere in the project, so the blocks were the incoherent half. Per the 2026-09-09 precedent that a rule needing a human to remember it is not a guardrail: `scripts/check-bot-policy.mjs` is wired as `postbuild` (standalone `npm run check:bots`) and fails the build when a vendor's AI agents disagree, when a retired token reappears, when `llms*.txt` invite ingestion while a trainer is blocked, when a token is declared in two groups, when a major agent is missing, when the Sitemap line drops the canonical `www` host, or when the `*` group grows a `Disallow` — that last one because under robots.txt precedence a named group ignores `*` entirely, so a wildcard-only `Disallow` would silently fail to apply to any AI bot. The script parses the PRERENDERED `robots.txt` and keeps its own independent vendor map; a guardrail that imports the module it guards only proves the module agrees with itself. Negative-tested all six branches, each exits 1. NOTE: `npm run lint` already exits 2 on this repo — no `eslint.config.*` exists at all — which is pre-existing and unrelated.

[2026-09-11] | AEO audit flagged "Indexable page blocked from some AI search bots" on whitneystevenson.com | Two findings, one already latent and one live. (1) The live defect is a **gap in the guardrail**, not in the data: `scripts/check-bot-policy.mjs` shipped six checks, and the one that looked like it covered this — the per-vendor consistency check — only catches a vendor disagreeing with *itself*. A policy that blocked `PerplexityBot` **and** `Perplexity-User` together is perfectly consistent and passed every check, while being exactly this finding. The previous lane's fix (2026-09-10) flipped every row to `allow: true`, so the rendered `robots.txt` blocked nothing at the time of the audit — but nothing prevented a future edit from re-blocking the citation crawlers wholesale, and the file that the guardrail is supposed to protect had no check for the thing this finding names. (2) Every group rendered as `Allow: /` with **no `Disallow:` line at all**. `Allow:` postdates the original robots.txt spec, which defined `Disallow:` only; a parser written to the older spec expects at least one `Disallow` per group and can discard a group that has none, leaving that bot with no record it recognises — a per-parser, invisible block that never shows up in a robots.txt linter. Separately, several assistant-search surfaces had no explicit group and fell through to `*`: `Google-CloudVertexBot` (Vertex AI grounding, not covered by `Google-Extended`), and the independent indexes Brave Leo and Kagi Assistant answer from (`Bravebot`, `Kagibot`). | RULE: "consistent" is not "permitted" — a guardrail against *contradiction* does not protect *access*, and the two need separate checks. `check-bot-policy.mjs` now hard-fails the build if **any** `ai-search` or `ai-user` agent is disallowed (check 7), whatever the training policy is doing, because those are the agents that produce a cited link back and blocking one costs a citation and buys nothing. It also fails if a group grants access with `Allow:` but ships no `Disallow:` line (check 8). `robots.ts` renders `disallow: [""]` on every permissive group so each one states its permission twice, in both robots.txt dialects — note the array: Next's serializer guards with a truthiness check, so a bare `disallow: ""` is silently dropped and renders nothing. Coverage for `Google-CloudVertexBot`, `Bravebot`, `Kagibot`, `Ai2Bot-Dolma`, `TikTokSpider` and `cohere-training-data-crawler` added to `BOT_POLICY`; the first three are in `MUST_COVER` alongside `Perplexity-User` and `DuckAssistBot`. Relying on `*` fall-through for an assistant crawler is not good enough — per the footgun already documented in `bot-policy.ts`, a named group ignores `*` entirely, so an agent is either named or it is at the mercy of whatever the wildcard happens to say later. Both new checks negative-tested: blocking both Perplexity tokens (vendor-consistent, so invisible to check 1) exits 1 on check 7, and stripping the empty `Disallow:` lines exits 1 on check 8. `npm run build` clean — 29 agents, 23 AI-purpose, 13 vendors. NOTE: `npm run lint` still exits 2 (no `eslint.config.*` in the repo at all) — pre-existing, unrelated, same as the 2026-09-10 entry.

[2026-09-12] | Ahrefs/GSC flagged "Title too long" (Warning severity, 3 URLs) | Same shape as the 2026-09-09 meta-description defect, one field doing two jobs, plus a multiplier nobody was counting. `Post.title` in `src/lib/posts.ts` served as both the **visible `<h1>`** on the post page (and the card heading on `/blog`, the homepage note list, and the footer) *and* as `metadata.title`. Long editorial headlines are a legitimate design choice — "What 300+ Tournaments at Presidio Golf Taught Me About Operations" is good copy — but a long `<title>` is an SEO defect, and because one field served both there was no way to shorten the tag without gutting visible copy. The multiplier: the root layout declares `template: "%s | Whitney Stevenson"`, which silently appends **20 characters** to every inherited title, so a 45-char source string renders as a 65-char tag and the source module looks innocent. Measured from the prerendered HTML, five routes were over the ~60-char SERP budget (85, 78, 73, 68 and the homepage at 70); Ahrefs reported 3 because its threshold fires above 70, so the two in the 60–70 band were latent and would have been the next report. | RULE: never let one string serve both visible copy and a meta tag — the 2026-09-09 rule, now applied to `<title>` as well as `<meta name="description">`. `Post` carries a separate required `metaTitle` (≤40 chars, because the template eats 20 of the 60) used for `metadata.title`, OpenGraph and Twitter; `title` stays the visible `<h1>` and its length is a design call. Visible headlines and BlogPosting `headline` were left untouched — no copy was shortened to fix this. Per the standing precedent that a rule needing a human to remember it is not a guardrail: `scripts/check-titles.mjs` parses the PRERENDERED HTML (the only place the template's +20 is visible, and it catches metadata composed anywhere — layout defaults, route `metadata`, `generateMetadata`) and fails the build if any crawlable route has zero or multiple `<title>` tags, one outside 30–60 chars, or one duplicated across routes. HTML entities are decoded before measuring — `&#x27;` is one character to a crawler, not six. Wired as `postbuild` alongside the link/meta/bot checks so it runs inside `npm run build` locally and on Vercel; standalone via `npm run check:titles`. All four failure branches negative-tested, each exits 1. NOTE: `_not-found.html` is excluded from this check only — Next ships its own built-in 404 `<title>` *in addition to* the layout's, so that route always has two and neither is ours to dedupe; it is served 404, never indexed, and Ahrefs reports this issue against indexable URLs only. It remains in scope for the meta-description check, which it passes. `npm run lint` still exits 2 (no `eslint.config.*` in the repo at all) — pre-existing, unrelated, same as the 2026-09-10 and 2026-09-11 entries.
